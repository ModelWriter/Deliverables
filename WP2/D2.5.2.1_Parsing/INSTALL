How to run pGen (The Parser module)

1. cd pGen

For the airbus use case, 2 different parsing configurations have been defined -- parse_airbus_excellex_simple_sentences_only (for executing on simple sentences) and parse_airbus_excellex_complex_sentences_only (for executing the parser on complex sentences). Accordingly, the following commands can be executed :

2. 
	A) ./run_parser.sh parse_airbus_excellex_simple_sentences_only
		The output is written to the folder resources/airbus/output/airbus_parse_output/excellex_simple_sentences_only/

	B) ./run_parser.sh parse_airbus_excellex_complex_sentences_only
		The output is written to the folder resources/airbus/output/airbus_parse_output/excellex_complex_sentences_only/
